# 景点增删功能设计方案

> 本文档描述在现有路线规划功能基础上，增加用户可对景点进行增删操作的完整设计方案。
> 生成时间：2026-05-03
> 更新：2026-05-03（修正搜索交互逻辑）

---

## 1. 功能概述

### 1.1 需求背景

当前系统生成的旅行路线，用户只能被动接受。为了增强用户体验，允许用户在已生成路线的基础上：
- 新增景点（通过搜索从高德 POI 库选择）
- 删除景点（点击地图标记实现）
- 调整后重新生成路线

### 1.2 设计原则

- 不修改原有 AI 生成流程
- 增删操作纯前端暂存，确认后才调用后端
- 新增景点可删除不可恢复，原始景点删除后可恢复
- 交互直观，通过地图标记实现

---

## 2. POI 数据结构

### 2.1 基础 POI 结构（BasicPOI）

用于搜索结果和用户新增景点，包含最少必要字段：

```typescript
interface BasicPOI {
  name: string;          // 景点名称，如"故宫博物院"
  location: string;      // 地址字符串或经纬度，如"116.397428,39.90923"
  lng: number;           // 经度，如 116.397428
  lat: number;           // 纬度，如 39.90923
  type: string;          // POI 类型，如"景点"
  rating?: number;       // 评分，如 4.8（可选）
  address?: string;      // 详细地址（可选）
}
```

### 2.2 路线 POI 结构（RoutePOI）

用于后端返回的完整路线景点，包含 AI 生成路线时附带的额外信息：

```typescript
interface RoutePOI extends BasicPOI {
  time?: string;         // 游览时间，如"09:00-12:00"
  duration?: string;    // 停留时长，如"3小时"
  reason?: string;      // 推荐理由
  transport?: string;   // 交通方式
  description?: string; // 详细描述
}
```

### 2.3 字段说明

| 字段 | 类型 | 必填 | 适用场景 |
|------|------|------|---------|
| name | string | ✅ | 所有 POI |
| location | string | ✅ | 所有 POI |
| lng | number | ✅ | 所有 POI |
| lat | number | ✅ | 所有 POI |
| type | string | ✅ | 所有 POI |
| rating | number | ○ | BasicPOI / RoutePOI |
| address | string | ○ | BasicPOI / RoutePOI |
| time | string | ○ | RoutePOI |
| duration | string | ○ | RoutePOI |
| reason | string | ○ | RoutePOI |
| transport | string | ○ | RoutePOI |
| description | string | ○ | RoutePOI |

### 2.4 字段缺失处理

- 详情窗口显示时，对不存在的字段显示为"暂无"
- 删除按钮不受影响

### 2.5 类型转换

```
用户搜索 → BasicPOI → 添加到路线
后端规划 → RoutePOI → 显示路线
```

从 BasicPOI 转为 RoutePOI 时，额外字段由后端在 replan 时补全。

---

## 3. 地图标记视觉设计

### 3.1 标记类型

| 类型 | 来源 | 初始状态 |
|------|------|---------|
| 原始景点 | 后端原路线 | 橙色实心圆形 + 序号 |
| 新增景点 | 用户搜索添加 | 绿色实心圆形 + "+"图标 |
| 搜索选中 | 用户在列表中选中（临时） | 蓝色虚线边框圆形 |

### 3.2 标记状态

| 状态 | 视觉表现 | 说明 |
|------|---------|------|
| 正常 | 基础样式（橙色/绿色） | 未被操作 |
| 选中 | 基础样式 + 白色高亮边框 | 待处理（可能删除） |
| 待删除 | 灰色 + 删除线 + 抖动动画 | 仅原始景点有此状态 |
| 已删除 | 移除（新增景点）或变灰（原始景点） | — |
| 搜索选中 | 蓝色虚线边框圆形 | 临时状态，地图上最多 1 个 |

### 3.3 标记样式定义

```css
/* 原始景点 - 正常 */
.original-poi {
  background: #f59e0b;  /* 橙色 */
  border-radius: 50%;
}

/* 原始景点 - 选中（待删除） */
.original-poi.selected {
  box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.8);
}

/* 原始景点 - 待删除 */
.original-poi.deleted {
  background: #9ca3af;  /* 灰色 */
  text-decoration: line-through;
  animation: shake 0.5s infinite;
}

/* 新增景点 - 正常 */
.user-add-poi {
  background: #22c55e;  /* 绿色 */
}

/* 新增景点 - 选中 */
.user-add-poi.selected {
  box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.8);
}

/* 搜索选中标记 - 临时（蓝色虚线） */
.search-selected-marker {
  background: transparent;
  border: 2px dashed #3b82f6;  /* 蓝色虚线 */
  border-radius: 50%;
}
```

---

## 4. 交互设计

### 4.1 搜索与选择流程

```
┌─────────────────────────────────────────────────────────────┐
│ 步骤1：搜索景点                                            │
│ 输入框输入关键词 → 点击搜索 → 显示搜索结果列表            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2：选择景点                                            │
│ 点击列表项 → 该 POI 成为"搜索选中"状态                    │
│   - 地图上显示 1 个蓝色虚线边框标记（位于 POI 实际坐标）  │
│   - 列表项高亮显示                                        │
│ 点击同一列表项 → 取消选择，蓝色虚线标记消失                │
│ 点击另一列表项 → 切换选择，蓝色虚线标记移到新 POI         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤3：添加景点                                            │
│ 选择 POI 后，点击"添加选中"按钮                            │
│   - POI 添加到 addedPois 列表                              │
│   - selectedPoi = null（蓝色虚线标记消失）                 │
│   - 列表项保持高亮 + 显示"已添加 ✓"                       │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 完整操作流程

```
┌─────────────────────────────────────────────────────────────┐
│ 步骤1：搜索景点                                            │
│ 输入框输入关键词 → 点击搜索 → 显示搜索结果列表            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2：选择景点                                            │
│ 点击列表项 → 地图显示 1 个蓝色虚线临时标记                 │
│ 再次点击同一列表项 → 取消选择，标记消失                    │
│ 点击另一列表项 → 标记切换到新的 POI                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤3：添加景点                                            │
│ 点击"添加选中"按钮 → POI 添加到已选列表                    │
│ 该列表项保持高亮 + "已添加 ✓"                             │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤4：删除/恢复景点                                       │
│ 点击路线中的标记（原始/新增）→ 切换"选中"状态             │
│ 点击"删除选中"按钮 → 弹出确认框                            │
│ 确认后：                                                   │
│   - 原始景点：变为"待删除"状态（灰色+删除线），可再点击恢复│
│   - 新增景点：直接移除                                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤5：查看详情                                            │
│ 点击标记 → 弹出信息窗口，显示名称、评分、地址等           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤6：确认重新规划                                        │
│ 点击"确认重新规划"按钮 → 调用 replan 接口 → 生成新路线    │
│ 新路线生成后，之前所有增删操作被应用                      │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 标记状态切换逻辑

```
原始景点（橙色）：
  正常 → 点击 → 选中（高亮）→ 点击"删除" → 待删除（灰色）→ 再次点击 → 恢复为正常

新增景点（绿色）：
  正常 → 点击 → 选中（高亮）→ 点击"删除" → 直接移除（不可恢复）

搜索选中（蓝色虚线）：
  无 → 点击列表项 → 蓝色虚线标记出现 → 点击"添加" → 添加到 addedPois
  蓝色虚线标记存在 → 再次点击同一列表项 → 取消选择，标记消失
```

### 4.4 信息窗口内容

详情窗口使用统一的 POI 结构显示，对不存在的字段显示为"暂无"：

```html
<div class="info-window">
  <div class="info-window-header">
    <span class="info-window-number">{{ index + 1 }}</span>
    <h3 class="info-window-title">{{ poi.name }}</h3>
  </div>
  <div class="info-window-body">
    <div class="info-row">
      <span class="info-label">评分:</span>
      {{ poi.rating || '暂无' }}
    </div>
    <div class="info-row">
      <span class="info-label">地址:</span>
      {{ poi.address || '暂无' }}
    </div>
    <div class="info-row">
      <span class="info-label">类型:</span>
      {{ poi.type || '暂无' }}
    </div>
    <!-- 以下字段仅 RoutePOI 才有 -->
    <div class="info-row" v-if="poi.time">
      <span class="info-label">游览时间:</span>
      {{ poi.time }}
    </div>
    <div class="info-row" v-if="poi.duration">
      <span class="info-label">停留时长:</span>
      {{ poi.duration }}
    </div>
    <div class="info-row" v-if="poi.reason">
      <span class="info-label">推荐理由:</span>
      {{ poi.reason }}
    </div>
  </div>
</div>
```

### 4.5 删除确认弹窗

```
┌────────────────────────────────────┐
│  确认删除                          │
│                                    │
│  确认删除以下 N 个景点？            │
│  - 故宫博物院                       │
│  - 天安门广场                       │
│  ...                               │
│                                    │
│  [取消]            [确认删除]       │
└────────────────────────────────────┘
```

---

## 5. 前端状态管理

### 5.1 状态结构

```typescript
interface TripState {
  // 原始路线（从后端获取的完整路线）
  originalRoutes: RoutePOI[];

  // 用户新增的景点（基础 POI）
  addedPois: BasicPOI[];

  // 用户选中的景点（用于删除）
  selectedPois: Set<string>;  // 通过 POI.name 或唯一 ID 标识

  // 待删除的原始景点（保留在 originalRoutes 中，标记状态）
  pendingDeletePois: Set<string>;

  // 搜索结果（所有搜索到的 POI）
  searchResults: BasicPOI[];

  // 当前在地图上显示的"搜索选中"标记（最多 1 个）
  selectedSearchPoi: BasicPOI | null;

  // 已被添加的 POI 名称集合（用于列表中显示"已添加"状态）
  addedPoiNames: Set<string>;
}
```

### 5.2 状态操作

| 操作 | 说明 |
|------|------|
| searchPoi(keyword) | 搜索 POI，更新 searchResults |
| selectSearchPoi(poi) | 切换搜索选中状态，地图显示蓝色虚线标记 |
| addSelectedToList() | 将 selectedSearchPoi 添加到 addedPois |
| selectPoi(name) | 切换路线中景点的选中状态（用于删除） |
| deleteSelected() | 删除选中的景点 |
| restorePoi(name) | 恢复待删除的原始景点 |
| confirmReplan() | 准备调用 replan |

### 5.3 状态流转（搜索与选择）

```
初始状态：
  searchResults = []
  selectedSearchPoi = null
  addedPois = []
  addedPoiNames = []

用户搜索"故宫"：
  searchResults = [POI-A, POI-B, POI-C]
  地图无蓝色虚线标记

用户点击列表项 A：
  selectedSearchPoi = POI-A
  地图显示 POI-A 位置的蓝色虚线标记
  列表项 A 高亮

用户再次点击列表项 A（取消选择）：
  selectedSearchPoi = null
  蓝色虚线标记消失
  列表项 A 取消高亮

用户点击列表项 B：
  selectedSearchPoi = POI-B
  蓝色虚线标记移到 POI-B 位置
  列表项 B 高亮

用户点击"添加选中"按钮：
  addedPois = [..., POI-B]
  addedPoiNames = ['POI-B']
  selectedSearchPoi = null
  蓝色虚线标记消失
  列表项 B 保持高亮 + 显示"已添加 ✓"

用户点击列表项 C：
  selectedSearchPoi = POI-C
  蓝色虚线标记移到 POI-C 位置
  列表项 C 高亮
  （列表项 B 仍高亮但显示"已添加 ✓"）
```

### 5.4 状态流转（删除与恢复）

```
初始状态：originalRoutes = [RoutePOI-A, RoutePOI-B, RoutePOI-C]
          addedPois = [BasicPOI-D]

用户点击 B（选中）：
  selectedPois = ['B']

用户点击"删除选中" → 确认：
  pendingDeletePois = ['B']
  B 变为灰色+删除线状态

用户点击 B（恢复）：
  pendingDeletePois = []
  B 恢复为正常橙色标记

用户点击"确认重新规划"：
  调用 replan({ pois: [RoutePOI-A, RoutePOI-C, BasicPOI-D] })
  后端补全 BasicPOI 的扩展字段，返回新的 RoutePOI 列表
```

---

## 6. 后端 API 设计

### 6.1 新增接口：POI 搜索

**端点**：`POST /api/pois/search`

**请求**：
```json
{
  "keyword": "故宫",
  "city": "beijing"
}
```

**响应**：
```json
{
  "success": true,
  "data": [
    {
      "name": "故宫博物院",
      "location": "116.397428,39.90923",
      "lng": 116.397428,
      "lat": 39.90923,
      "type": "景点",
      "rating": 4.8,
      "address": "北京市东城区景山前街4号"
    },
    {
      "name": "故宫角楼",
      "location": "116.397864,39.911236",
      "lng": 116.397864,
      "lat": 39.911236,
      "type": "景点",
      "rating": 4.6,
      "address": "北京市东城区景山前街4号"
    }
  ]
}
```

### 6.2 新增接口：重新规划

**端点**：`POST /api/agent/replan`

**请求**：
```json
{
  "city": "beijing",
  "startTime": "2026-05-03 09:00",
  "endTime": "2026-05-03 18:00",
  "preferences": "亲子游，喜欢自然风光",
  "pois": [
    {
      "name": "故宫博物院",
      "location": "116.397428,39.90923",
      "lng": 116.397428,
      "lat": 39.90923,
      "type": "景点"
    },
    {
      "name": "天坛公园",
      "location": "116.410642,39.880325",
      "lng": 116.410642,
      "lat": 39.880325,
      "type": "公园"
    }
  ]
}
```

**请求说明**：
- `pois` 数组为用户确认的景点列表
- 支持 BasicPOI 结构（用户新增景点）和 RoutePOI 结构（原始景点）
- 后端将这些景点作为必经点，调用高德路径规划 API 生成最优顺序
- 后端补全每个 POI 的扩展字段（time, duration, reason, transport, description）

**响应**：
```json
{
  "success": true,
  "data": {
    "id": "task_123456",
    "status": "pending"
  }
}
```

### 6.3 轮询接口（复用现有）

**端点**：`GET /api/agent/task/{id}`

**响应**：
```json
{
  "success": true,
  "data": {
    "id": "task_123456",
    "status": "completed",
    "result": {
      "routes": [...],
      "summary": "..."
    }
  }
}
```

---

## 7. 功能限制

### 7.1 景点数量限制

- 用户新增景点数 ≤ 原始景点数 + 3
- 防止路径规划复杂度指数增长

### 7.2 搜索限制

- 搜索频率限制：每秒最多 1 次
- 搜索结果最多返回 10 条

---

## 8. 前端组件变更

### 8.1 新增组件

| 组件 | 说明 |
|------|------|
| `PoiSearchBar.vue` | 搜索框组件，包含输入框、搜索结果列表、选择状态管理 |

### 8.2 修改组件

| 组件 | 变更说明 |
|------|---------|
| `AmapContainer.vue` | 支持显示单个"搜索选中"蓝色虚线标记；标记点击事件 |
| `ItineraryOutput.vue` | 增加删除按钮、增加选中列表区域 |
| `HomeView.vue` | 管理 selectedSearchPoi、addedPois 等状态；"添加选中"和"确认重新规划"按钮 |

### 8.3 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│ [搜索框                          ] [添加选中] [确认重新规划] │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                      地图区域                                │
│              （橙色原始景点、绿色新增景点、蓝色搜索选中）      │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│ 搜索结果列表                                                 │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ [列表项 A - 高亮，已添加 ✓]                    [+添加]  │  │
│ │ [列表项 B - 高亮]                                 [+添加]  │  │
│ │ [列表项 C - 未选中]                               [+添加]  │  │
│ └────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│                     路线列表区域                              │
│              （显示所有景点，含状态）                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 9. 实现顺序

### 阶段一：后端 API
1. 实现 `/api/pois/search` 接口 ✅
2. 实现 `/api/agent/replan` 接口 ✅

### 阶段二：前端基础
1. 实现 `PoiSearchBar.vue` 组件 ✅
2. 修改 `AmapContainer.vue` 支持临时标记 ✅

### 阶段三：交互实现（修正搜索选择逻辑）
1. 修改 PoiSearchBar.vue 选择状态管理
2. 修改 AmapContainer.vue 只显示 1 个蓝色虚线标记
3. 实现标记的选中和删除逻辑
4. 实现恢复功能
5. 实现确认重新规划流程

### 阶段四：UI 优化
1. 完善弹窗和动画
2. 处理边界情况

---

## 10. 文档更新清单

功能实现完成后，需同步更新以下文档：

- [ ] `wiki.md` - 新增 API 接口说明（第5节）
- [ ] `wiki.md` - 更新数据模型（POI 结构）
- [ ] `wiki.md` - 更新任务执行流程

---

*最后更新：2026-05-03*